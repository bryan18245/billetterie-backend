---
deployment:
  tasks:
    - export DEPLOYPATH=/home/fonannco/billetterie-backend/
    - /bin/cp -R app $DEPLOYPATH
    - /bin/cp -R config $DEPLOYPATH
    - /bin/cp -R routes $DEPLOYPATH
    - /bin/cp -R resources $DEPLOYPATH
    - /bin/cp -R public $DEPLOYPATH
